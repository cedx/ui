@namespace Belin.UI.Components

<type-ahead delay="@Delay" list="@List" minLength="@MinLength">
	<datalist id="@List"><!-- Options --></datalist>
</type-ahead>

@code {
	/// <summary>
	/// The delay in milliseconds to wait before triggering autocomplete suggestions.
	/// </summary>
	[Parameter]
	public int Delay { get; set => field = Math.Max(0, value); } = 300;

	/// <summary>
	/// The data list identifier.
	/// </summary>
	[Parameter, EditorRequired]
	public required string List { get; set; }

	/// <summary>
	/// The minimum character length needed before triggering autocomplete suggestions.
	/// </summary>
	[Parameter]
	public int MinLength { get; set => field = Math.Max(1, value); } = 2;
}

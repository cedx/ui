name: continuous-integration
on: push

defaults:
  run:
    shell: pwsh

jobs:
  client:
    runs-on: windows-latest
    steps:
      - name: Fetch sources
        uses: actions/checkout@v6
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          cache: npm
          node-version: latest
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: ./Invoke.ps1 Client/Test
  server:
    runs-on: windows-latest
    steps:
      - name: Fetch sources
        uses: actions/checkout@v6
      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10
      - name: Run tests
        run: ./Invoke.ps1 Server/Test
